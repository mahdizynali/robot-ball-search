#ifndef UTIL_HPP
#define UTIL_HPP

#include "Configs.hpp"

double modAngle(double);
vector <double> poseGlobal(vector <double>, vector <double>);
vector <double> poseRelative(vector <double>, vector <double>);

#endif // UTIL_HPP